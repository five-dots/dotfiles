# Disable default bindings

## preifx

unbind-key C-b   # Send the prefix key
unbind-key C-o   # Rotate through the panes
unbind-key C-z   # Suspend the current client
unbind-key Space # Select next layout
unbind-key !     # Break pane to a new window
unbind-key \"    # Split window vertically
unbind-key \#    # List all paste buffers
unbind-key \$    # Rename current session
unbind-key \%    # Split window horizontally
unbind-key &     # Kill current window
unbind-key \'    # Prompt for window index to select
unbind-key (     # Switch to previous client
unbind-key )     # Switch to next client
unbind-key ,     # Rename current window
unbind-key -     # Delete the most recent paste buffer
unbind-key .     # Move the current window
unbind-key /     # Describe key binding
unbind-key 0     # Select window 0
unbind-key 1     # Select window 1
unbind-key 2     # Select window 2
unbind-key 3     # Select window 3
unbind-key 4     # Select window 4
unbind-key 5     # Select window 5
unbind-key 6     # Select window 6
unbind-key 7     # Select window 7
unbind-key 8     # Select window 8
unbind-key 9     # Select window 9
unbind-key :     # Prompt for a command
unbind-key \;    # Move to the previously active pane
unbind-key =     # Choose a paste buffer from a list
unbind-key ?     # List key bindings
unbind-key C     # Customize options
unbind-key D     # Choose and detach a client from a list
unbind-key E     # Spread panes out evenly
unbind-key L     # Switch to the last client
unbind-key M     # Clear the marked pane
unbind-key [     # Enter copy mode
unbind-key ]     # Paste the most recent paste buffer
unbind-key c     # Create a new window
unbind-key d     # Detach the current client
unbind-key f     # Search for a pane
unbind-key i     # Display window information
unbind-key l     # Select the previously current window
unbind-key m     # Toggle the marked pane
unbind-key n     # Select the next window
unbind-key o     # Select the next pane
unbind-key p     # Select the previous window
unbind-key q     # Display pane numbers
unbind-key r     # Redraw the current client
unbind-key s     # Choose a session from a list
unbind-key t     # Show a clock
unbind-key w     # Choose a window from a list
unbind-key x     # Kill the active pane
unbind-key z     # Zoom the active pane
unbind-key \{    # Swap the active pane with the pane above
unbind-key \}    # Swap the active pane with the pane below
unbind-key \~    # Show messages
unbind-key PPage # Enter copy mode and scroll up
unbind-key M-1   # Set the even-horizontal layout
unbind-key M-2   # Set the even-vertical layout
unbind-key M-3   # Set the main-horizontal layout
unbind-key M-4   # Set the main-vertical layout
unbind-key M-5   # Select the tiled layout
unbind-key M-n   # Select the next window with an alert
unbind-key M-o   # Rotate through the panes in reverse
unbind-key M-p   # Select the previous window with an alert

## preifx (-r)

unbind-key DC      # Reset so the visible part of the window follows the cursor
unbind-key Up      # Select the pane above the active pane
unbind-key Down    # Select the pane below the active pane
unbind-key Left    # Select the pane to the left of the active pane
unbind-key Right   # Select the pane to the right of the active pane
unbind-key C-Up    # Resize the pane up
unbind-key C-Down  # Resize the pane down
unbind-key C-Left  # Resize the pane left
unbind-key C-Right # Resize the pane right
unbind-key M-Up    # Resize the pane up by 5
unbind-key M-Down  # Resize the pane down by 5
unbind-key M-Left  # Resize the pane left by 5
unbind-key M-Right # Resize the pane right by 5
unbind-key S-Up    # Move the visible part of the window up
unbind-key S-Down  # Move the visible part of the window down
unbind-key S-Left  # Move the visible part of the window left
unbind-key S-Right # Move the visible part of the window right

## copy-mode

unbind-key -T copy-mode C-Space           #  send-keys -X begin-selection
unbind-key -T copy-mode C-a               #  send-keys -X start-of-line
unbind-key -T copy-mode C-b               #  send-keys -X cursor-left
unbind-key -T copy-mode C-c               #  send-keys -X cancel
unbind-key -T copy-mode C-e               #  send-keys -X end-of-line
unbind-key -T copy-mode C-f               #  send-keys -X cursor-right
unbind-key -T copy-mode C-g               #  send-keys -X clear-selection
unbind-key -T copy-mode C-k               #  send-keys -X copy-pipe-end-of-line-and-cancel
unbind-key -T copy-mode C-n               #  send-keys -X cursor-down
unbind-key -T copy-mode C-p               #  send-keys -X cursor-up
unbind-key -T copy-mode C-r               #  command-prompt -i -I "#{pane_search_string}" -T search -p "(search up)" { send-keys -X search-backward-incremental "%%" }
unbind-key -T copy-mode C-s               #  command-prompt -i -I "#{pane_search_string}" -T search -p "(search down)" { send-keys -X search-forward-incremental "%%" }
unbind-key -T copy-mode C-v               #  send-keys -X page-down
unbind-key -T copy-mode C-w               #  send-keys -X copy-pipe-and-cancel
unbind-key -T copy-mode Escape            #  send-keys -X cancel
unbind-key -T copy-mode Space             #  send-keys -X page-down
unbind-key -T copy-mode ,                 #  send-keys -X jump-reverse
unbind-key -T copy-mode \;                #  send-keys -X jump-again
unbind-key -T copy-mode F                 #  command-prompt -1 -p "(jump backward)" { send-keys -X jump-backward "%%" }
unbind-key -T copy-mode N                 #  send-keys -X search-reverse
unbind-key -T copy-mode P                 #  send-keys -X toggle-position
unbind-key -T copy-mode R                 #  send-keys -X rectangle-toggle
unbind-key -T copy-mode T                 #  command-prompt -1 -p "(jump to backward)" { send-keys -X jump-to-backward "%%" }
unbind-key -T copy-mode X                 #  send-keys -X set-mark
unbind-key -T copy-mode f                 #  command-prompt -1 -p "(jump forward)" { send-keys -X jump-forward "%%" }
unbind-key -T copy-mode g                 #  command-prompt -p "(goto line)" { send-keys -X goto-line "%%" }
unbind-key -T copy-mode n                 #  send-keys -X search-again
unbind-key -T copy-mode q                 #  send-keys -X cancel
unbind-key -T copy-mode r                 #  send-keys -X refresh-from-pane
unbind-key -T copy-mode t                 #  command-prompt -1 -p "(jump to forward)" { send-keys -X jump-to-forward "%%" }
unbind-key -T copy-mode MouseDown1Pane    #  select-pane
unbind-key -T copy-mode MouseDrag1Pane    #  select-pane \; send-keys -X begin-selection
unbind-key -T copy-mode MouseDragEnd1Pane #  send-keys -X copy-pipe-and-cancel
unbind-key -T copy-mode WheelUpPane       #  select-pane \; send-keys -X -N 5 scroll-up
unbind-key -T copy-mode WheelDownPane     #  select-pane \; send-keys -X -N 5 scroll-down
unbind-key -T copy-mode DoubleClick1Pane  #  select-pane \; send-keys -X select-word \; run-shell -d 0.3 \; send-keys -X copy-pipe-and-cancel
unbind-key -T copy-mode TripleClick1Pane  #  select-pane \; send-keys -X select-line \; run-shell -d 0.3 \; send-keys -X copy-pipe-and-cancel
unbind-key -T copy-mode Home              #  send-keys -X start-of-line
unbind-key -T copy-mode End               #  send-keys -X end-of-line
unbind-key -T copy-mode NPage             #  send-keys -X page-down
unbind-key -T copy-mode PPage             #  send-keys -X page-up
unbind-key -T copy-mode Up                #  send-keys -X cursor-up
unbind-key -T copy-mode Down              #  send-keys -X cursor-down
unbind-key -T copy-mode Left              #  send-keys -X cursor-left
unbind-key -T copy-mode Right             #  send-keys -X cursor-right
unbind-key -T copy-mode M-C-b             #  send-keys -X previous-matching-bracket
unbind-key -T copy-mode M-C-f             #  send-keys -X next-matching-bracket
unbind-key -T copy-mode M-1               #  command-prompt -N -I 1 -p (repeat) { send-keys -N "%%" }
unbind-key -T copy-mode M-2               #  command-prompt -N -I 2 -p (repeat) { send-keys -N "%%" }
unbind-key -T copy-mode M-3               #  command-prompt -N -I 3 -p (repeat) { send-keys -N "%%" }
unbind-key -T copy-mode M-4               #  command-prompt -N -I 4 -p (repeat) { send-keys -N "%%" }
unbind-key -T copy-mode M-5               #  command-prompt -N -I 5 -p (repeat) { send-keys -N "%%" }
unbind-key -T copy-mode M-6               #  command-prompt -N -I 6 -p (repeat) { send-keys -N "%%" }
unbind-key -T copy-mode M-7               #  command-prompt -N -I 7 -p (repeat) { send-keys -N "%%" }
unbind-key -T copy-mode M-8               #  command-prompt -N -I 8 -p (repeat) { send-keys -N "%%" }
unbind-key -T copy-mode M-9               #  command-prompt -N -I 9 -p (repeat) { send-keys -N "%%" }
unbind-key -T copy-mode M-<               #  send-keys -X history-top
unbind-key -T copy-mode M->               #  send-keys -X history-bottom
unbind-key -T copy-mode M-R               #  send-keys -X top-line
unbind-key -T copy-mode M-b               #  send-keys -X previous-word
unbind-key -T copy-mode M-f               #  send-keys -X next-word-end
unbind-key -T copy-mode M-m               #  send-keys -X back-to-indentation
unbind-key -T copy-mode M-r               #  send-keys -X middle-line
unbind-key -T copy-mode M-v               #  send-keys -X page-up
unbind-key -T copy-mode M-w               #  send-keys -X copy-pipe-and-cancel
unbind-key -T copy-mode M-x               #  send-keys -X jump-to-mark
unbind-key -T copy-mode "M-{"             #  send-keys -X previous-paragraph
unbind-key -T copy-mode "M-}"             #  send-keys -X next-paragraph
unbind-key -T copy-mode M-Up              #  send-keys -X halfpage-up
unbind-key -T copy-mode M-Down            #  send-keys -X halfpage-down
unbind-key -T copy-mode C-Up              #  send-keys -X scroll-up
unbind-key -T copy-mode C-Down            #  send-keys -X scroll-down

## copy-mode-vi

# unbind-key -T copy-mode-vi C-b               # send-keys -X page-up
unbind-key -T copy-mode-vi C-c               # send-keys -X cancel
# unbind-key -T copy-mode-vi C-d               # send-keys -X halfpage-down
# unbind-key -T copy-mode-vi C-e               # send-keys -X scroll-down
# unbind-key -T copy-mode-vi C-f               # send-keys -X page-down
unbind-key -T copy-mode-vi C-h               # send-keys -X cursor-left
unbind-key -T copy-mode-vi C-j               # send-keys -X copy-pipe-and-cancel
unbind-key -T copy-mode-vi Enter             # send-keys -X copy-pipe-and-cancel
# unbind-key -T copy-mode-vi C-u               # send-keys -X halfpage-up
unbind-key -T copy-mode-vi C-v               # send-keys -X rectangle-toggle
# unbind-key -T copy-mode-vi C-y               # send-keys -X scroll-up
# unbind-key -T copy-mode-vi Escape            # send-keys -X clear-selection
unbind-key -T copy-mode-vi Space             # send-keys -X begin-selection
# unbind-key -T copy-mode-vi \#                # send-keys -FX search-backward "#{copy_cursor_word}"
# unbind-key -T copy-mode-vi \$                # send-keys -X end-of-line
# unbind-key -T copy-mode-vi \%                # send-keys -X next-matching-bracket
# unbind-key -T copy-mode-vi *                 # send-keys -FX search-forward "#{copy_cursor_word}"
# unbind-key -T copy-mode-vi ,                 # send-keys -X jump-reverse
# unbind-key -T copy-mode-vi /                 # command-prompt -T search -p "(search down)" { send-keys -X search-forward "%%" }
# unbind-key -T copy-mode-vi 0                 # send-keys -X start-of-line
# unbind-key -T copy-mode-vi 1                 # command-prompt -N -I 1 -p (repeat) { send-keys -N "%%" }
# unbind-key -T copy-mode-vi 2                 # command-prompt -N -I 2 -p (repeat) { send-keys -N "%%" }
# unbind-key -T copy-mode-vi 3                 # command-prompt -N -I 3 -p (repeat) { send-keys -N "%%" }
# unbind-key -T copy-mode-vi 4                 # command-prompt -N -I 4 -p (repeat) { send-keys -N "%%" }
# unbind-key -T copy-mode-vi 5                 # command-prompt -N -I 5 -p (repeat) { send-keys -N "%%" }
# unbind-key -T copy-mode-vi 6                 # command-prompt -N -I 6 -p (repeat) { send-keys -N "%%" }
# unbind-key -T copy-mode-vi 7                 # command-prompt -N -I 7 -p (repeat) { send-keys -N "%%" }
# unbind-key -T copy-mode-vi 8                 # command-prompt -N -I 8 -p (repeat) { send-keys -N "%%" }
# unbind-key -T copy-mode-vi 9                 # command-prompt -N -I 9 -p (repeat) { send-keys -N "%%" }
# unbind-key -T copy-mode-vi :                 # command-prompt -p "(goto line)" { send-keys -X goto-line "%%" }
# unbind-key -T copy-mode-vi \;                # send-keys -X jump-again
# unbind-key -T copy-mode-vi ?                 # command-prompt -T search -p "(search up)" { send-keys -X search-backward "%%" }
# unbind-key -T copy-mode-vi A                 # send-keys -X append-selection-and-cancel
# unbind-key -T copy-mode-vi B                 # send-keys -X previous-space
# unbind-key -T copy-mode-vi D                 # send-keys -X copy-pipe-end-of-line-and-cancel
# unbind-key -T copy-mode-vi E                 # send-keys -X next-space-end
# unbind-key -T copy-mode-vi F                 # command-prompt -1 -p "(jump backward)" { send-keys -X jump-backward "%%" }
# unbind-key -T copy-mode-vi G                 # send-keys -X history-bottom
# unbind-key -T copy-mode-vi H                 # send-keys -X top-line
# unbind-key -T copy-mode-vi J                 # send-keys -X scroll-down
# unbind-key -T copy-mode-vi K                 # send-keys -X scroll-up
# unbind-key -T copy-mode-vi L                 # send-keys -X bottom-line
# unbind-key -T copy-mode-vi M                 # send-keys -X middle-line
# unbind-key -T copy-mode-vi N                 # send-keys -X search-reverse
# unbind-key -T copy-mode-vi P                 # send-keys -X toggle-position
# unbind-key -T copy-mode-vi T                 # command-prompt -1 -p "(jump to backward)" { send-keys -X jump-to-backward "%%" }
# unbind-key -T copy-mode-vi V                 # send-keys -X select-line
# unbind-key -T copy-mode-vi W                 # send-keys -X next-space
# unbind-key -T copy-mode-vi X                 # send-keys -X set-mark
# unbind-key -T copy-mode-vi ^                 # send-keys -X back-to-indentation
# unbind-key -T copy-mode-vi b                 # send-keys -X previous-word
# unbind-key -T copy-mode-vi e                 # send-keys -X next-word-end
# unbind-key -T copy-mode-vi f                 # command-prompt -1 -p "(jump forward)" { send-keys -X jump-forward "%%" }
# unbind-key -T copy-mode-vi g                 # send-keys -X history-top
# unbind-key -T copy-mode-vi h                 # send-keys -X cursor-left
# unbind-key -T copy-mode-vi j                 # send-keys -X cursor-down
# unbind-key -T copy-mode-vi k                 # send-keys -X cursor-up
# unbind-key -T copy-mode-vi l                 # send-keys -X cursor-right
# unbind-key -T copy-mode-vi n                 # send-keys -X search-again
# unbind-key -T copy-mode-vi o                 # send-keys -X other-end
# unbind-key -T copy-mode-vi q                 # send-keys -X cancel
# unbind-key -T copy-mode-vi r                 # send-keys -X refresh-from-pane
# unbind-key -T copy-mode-vi t                 # command-prompt -1 -p "(jump to forward)" { send-keys -X jump-to-forward "%%" }
unbind-key -T copy-mode-vi v                 # send-keys -X rectangle-toggle
# unbind-key -T copy-mode-vi w                 # send-keys -X next-word
# unbind-key -T copy-mode-vi z                 # send-keys -X scroll-middle
# unbind-key -T copy-mode-vi \{                # send-keys -X previous-paragraph
# unbind-key -T copy-mode-vi \}                # send-keys -X next-paragraph
# unbind-key -T copy-mode-vi MouseDown1Pane    # select-pane
# unbind-key -T copy-mode-vi MouseDrag1Pane    # select-pane \; send-keys -X begin-selection
# unbind-key -T copy-mode-vi MouseDragEnd1Pane # send-keys -X copy-pipe-and-cancel
# unbind-key -T copy-mode-vi WheelUpPane       # select-pane \; send-keys -X -N 5 scroll-up
# unbind-key -T copy-mode-vi WheelDownPane     # select-pane \; send-keys -X -N 5 scroll-down
# unbind-key -T copy-mode-vi DoubleClick1Pane  # select-pane \; send-keys -X select-word \; run-shell -d 0.3 \; send-keys -X copy-pipe-and-cancel
# unbind-key -T copy-mode-vi TripleClick1Pane  # select-pane \; send-keys -X select-line \; run-shell -d 0.3 \; send-keys -X copy-pipe-and-cancel
unbind-key -T copy-mode-vi BSpace            # send-keys -X cursor-left
# unbind-key -T copy-mode-vi Home              # send-keys -X start-of-line
# unbind-key -T copy-mode-vi End               # send-keys -X end-of-line
# unbind-key -T copy-mode-vi NPage             # send-keys -X page-down
# unbind-key -T copy-mode-vi PPage             # send-keys -X page-up
# unbind-key -T copy-mode-vi Up                # send-keys -X cursor-up
# unbind-key -T copy-mode-vi Down              # send-keys -X cursor-down
# unbind-key -T copy-mode-vi Left              # send-keys -X cursor-left
# unbind-key -T copy-mode-vi Right             # send-keys -X cursor-right
# unbind-key -T copy-mode-vi M-x               # send-keys -X jump-to-mark
# unbind-key -T copy-mode-vi C-Up              # send-keys -X scroll-up
# unbind-key -T copy-mode-vi C-Down            # send-keys -X scroll-down

## Tmux Plugin Manager

unbind-key I
unbind-key U
unbind-key M-u

## Tmux Resurrect

unbind-key C-r
unbind-key C-s
